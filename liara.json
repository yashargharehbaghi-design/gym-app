{
  "platform": "python",
  "app": "my-fitness-app",
  "port": 8000,
  "args": [
    "sh",
    "-c",
    "export HOME=/tmp && export MPLCONFIGDIR=/tmp && streamlit run app.py --server.port=8000 --server.address=0.0.0.0 --server.enableCORS=false --server.enableXsrfProtection=false"
  ]
}
